/**
 * Multiplayer Agent Workspace — server entry point.
 *
 * This is a scaffold placeholder. Real-time WebSocket gateway, room manager,
 * and services are implemented in later tasks.
 */
export function serverVersion(): string {
  return "0.1.0";
}
