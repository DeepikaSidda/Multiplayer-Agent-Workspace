/**
 * @maw/shared — shared domain types, constants, and the WebSocket event
 * contract for the Multiplayer Agent Workspace. Imported by both the server
 * (`@maw/server`) and the client (`@maw/client`).
 */

export * from "./types.js";
export * from "./constants.js";
export * from "./events.js";
